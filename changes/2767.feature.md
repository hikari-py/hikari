Add support for the subscriptions API: the `Subscription` model, `RESTClient.fetch_sku_subscriptions` and `RESTClient.fetch_sku_subscription`, and the `SubscriptionCreateEvent`, `SubscriptionUpdateEvent` and `SubscriptionDeleteEvent` gateway events
