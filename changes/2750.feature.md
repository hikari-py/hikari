Add `RESTClient.fetch_entitlement` and `RESTClient.consume_entitlement`, the `Entitlement.is_consumed` field, and the missing `skus` and `exclude_deleted` parameters to `RESTClient.fetch_entitlements`
