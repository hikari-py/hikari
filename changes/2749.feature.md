Add support for role colors: the `ColorGradient` model, the `Role.role_colors` field, the `role_colors` parameter in `RESTClient.create_role` and `RESTClient.edit_role`, and the `ENHANCED_ROLE_COLORS` guild feature
