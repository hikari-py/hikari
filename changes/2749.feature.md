Add support for role colors: the `ColorGradient` model, the `Role.colors` field, support for passing a `ColorGradient` as the `color` in `RESTClient.create_role` and `RESTClient.edit_role`, and the `ENHANCED_ROLE_COLORS` guild feature
