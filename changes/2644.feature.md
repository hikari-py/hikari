Add support for the `RATE_LIMITED` gateway event: the shard now logs a warning when an operation gets rate limited and the new `ShardRateLimitedEvent` and `RequestGuildMembersRateLimitedEvent` events are dispatched so applications can react to it
