- Reset the gateway rate limit window on every new connection, preventing `4008` disconnects after re-identifying.
- Payloads (such as member chunk requests) that were waiting on the gateway rate limit when the connection dropped are now sent once the shard reconnects, instead of being silently dropped.
