- Reset the gateway rate limit window on every new connection, preventing `4008` disconnects after re-identifying.
- Payloads (such as member chunk requests) waiting on the gateway rate limit when the connection dropped are now sent after resuming instead of being silently dropped. If a new session is started instead, they fail with `ComponentStateConflictError`.
