- Reset the gateway send rate limit window on every new websocket connection, preventing `4008` rate limit disconnects after a re-identify with many guilds.
- Log a warning when payloads still queued for a previous connection (such as member chunk requests) are discarded on resume, and raise `ComponentStateConflictError` instead of failing silently when a payload is released after the shard disconnected.
