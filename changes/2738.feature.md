Add `RESTClient.edit_application` to edit the current application, along with a new `ApplicationEventWebhookStatus` enum and `event_webhooks_url`, `event_webhooks_status` and `event_webhooks_types` fields on `Application`
