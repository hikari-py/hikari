Add `RESTClient.edit_application` to edit the current application, along with new `ApplicationEventWebhookStatus` and `ApplicationEventWebhookType` enums and `event_webhooks_url`, `event_webhooks_status` and `event_webhooks_types` fields on `Application`
