Casting an unknown value to an enum or flag type now returns an unknown member which keeps hold of the raw value instead of returning the value unchanged, and casting a value of the wrong type now raises `TypeError`; model fields and REST parameters are now typed with only the enum/flag type instead of a union with its raw type
