True-up the invites module: add the `Invite.type` field with the new `InviteType` enum, the `Invite.flags` field with the new `InviteFlags` enum, the `Invite.guild_scheduled_event` field, the `Invite.roles` field with the new `InviteRole` model, the `Invite.role_ids` field, and the missing `scheduled_event` parameter to `RESTClient.fetch_invite`
