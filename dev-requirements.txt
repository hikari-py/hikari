async-timeout==3.0.1
coverage==5.0.3
nox
pytest==5.3.5
pytest-asyncio==0.10.0
pytest-cov==2.8.1
pytest-html==2.0.1
pytest-xdist==1.31.0

# We use this to get pretty output, but for now I have disabled it to see how it impacts test execution speed.
# pytest-testdox==1.2.1
